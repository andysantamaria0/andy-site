import { vialoureIcon, ICON_SIZE } from '../../lib/brand/vialoure';

export const size = ICON_SIZE;
export const contentType = 'image/png';

export default function Icon() {
  return vialoureIcon();
}
